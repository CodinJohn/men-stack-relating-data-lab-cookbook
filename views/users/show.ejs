<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %>'s Pantry</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= user.username %>'s Pantry</h1>
    <ul>
        <% if (pantryItems.length > 0) { %>
            <% pantryItems.forEach(item => { %>
                <li><%= item.text %> - <%= item.isInPantry ? 'In Pantry' : 'Not in Pantry' %></li>
            <% }) %>
        <% } else { %>
            <li>This pantry is empty.</li>
        <% } %>
    </ul>
    <a href="/users">Back to Community</a>
</body>
</html>

<!-- Collaberated with Anitra & Samantha -->